### frontend (terminal 1)
```
cd frontend
npm install
npm start
```

### backend (terminal 2)
```
cd backend
pip install -r requirements.txt
python app.py
```

### live mock logs (terminal 3)
```
cd backend
python mockserver.py demo
```